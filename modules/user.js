const express = require('express');
const router = express.Router();

// Données simulées
let users = [
  const express = require('express');
const router = express.Router();

// Données simulées
let users = [
  { id: 1, name: 'Alice' },
  { id: 2, name: 'Bob' }
];
];